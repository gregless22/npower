// get the model
// const model = require (appRoot + '/models/Model.js')


module.export = {
    create: (req, res, next) => {
        
    }

}

module.exports = (model) => {
    this.model = model
}